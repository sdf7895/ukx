import 'package:flutter/material.dart';

class DefaultTabs extends StatelessWidget {
  const DefaultTabs({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
